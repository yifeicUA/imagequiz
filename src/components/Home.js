//import App from "../App";
import { Container } from "react-bootstrap";
import { Row } from "react-bootstrap";
const Home = (props) => {
    return (
        <p>Hello from the home page</p>
    );
}
export default Home;